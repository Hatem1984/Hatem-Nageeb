name: Diagnosis share patch
on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/diagnosis-share-patch.yml
      - tools/diagnosis_share_patch.py
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python tools/diagnosis_share_patch.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          rm -f .github/workflows/diagnosis-share-patch.yml tools/diagnosis_share_patch.py
          git add -A
          git commit -m "Improve diagnosis Messenger handoff"
          git push
